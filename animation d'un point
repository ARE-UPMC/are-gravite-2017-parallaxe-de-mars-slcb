from __future__ import divisionfrom scipy import *                 from pylab import *import os%matplotlib notebook #initialisationtini = 0tfin = 2Npas = 100#Positions Successivesdef Positions(t):    x = 0.5*cos(2*pi*t)    y = 0.5*sin(2*pi*t)    return [x, y]# Construction d'une série d'images et de leur assemblage dans une animationfor k in range(Npas):    t = tini + (tfin-tini)*k/(Npas-1)    plot(Positions(t)[0], Positions(t)[1], 'o', color='b')    axis([-1, 1, -1, 1])    filename = 'fichierTemp'+str('%02d' %k)+'.pdf'    savefig(filename)    clf()show()
